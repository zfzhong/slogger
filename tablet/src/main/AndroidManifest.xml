<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"/>

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />


    <!-- Needed by the connectedDevice foreground-service type this app
         overrides the library's declaration with, below. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Slogger">
        <activity
            android:name="com.application.tablet.ConfigScrollingActivity"
            android:exported="false"
            android:label="@string/title_activity_config_scrolling"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity
            android:name="com.application.tablet.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.Slogger">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          The library declares this service with foregroundServiceType
          "location", which is what the watch app has always used and still
          uses. This app targets SDK 34, where location is a "while-in-use"
          type: a foreground service of that type STARTED FROM THE BACKGROUND
          throws SecurityException, and the scheduled start does exactly that -
          it killed the app whenever logging began while the user was in
          another app. connectedDevice describes what the service does here
          (its own sensors, and BLE to devices nearby), carries no while-in-use
          restriction and no daily limit.

          Overridden HERE rather than in the library, so the watch app - which
          targets 33, where none of this applies - keeps the manifest it was
          tested with.
        -->
        <service android:name="com.application.sloggerlib.SensorLoggingService"
            android:foregroundServiceType="connectedDevice"
            tools:replace="android:foregroundServiceType" />

        <receiver
            android:name="com.application.sloggerlib.StartLoggingAlarmReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        <receiver
            android:name="com.application.sloggerlib.StopLoggingAlarmReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>